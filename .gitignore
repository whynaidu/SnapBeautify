# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# electron build output
/dist/
/dist-electron/

# Capacitor
# Note: android/ and ios/ folders should be committed (they contain source files)
# Each has its own .gitignore for build artifacts

# APK/AAB files (Android builds)
*.apk
*.aab

# Keystore files (security - never commit these!)
*.keystore
*.jks
keystore.properties

# iOS build output (if iOS is added)
ios/App/Pods/
ios/App/build/
ios/App/*.xcworkspace/xcuserdata/
ios/App/*.xcodeproj/xcuserdata/
ios/App/DerivedData/

*storybook.log
storybook-static
